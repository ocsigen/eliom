To update the API documentation:
make doccp
git checkout wikidoc
rm -rf doc/dev/api
cp -r _build/doc/dev/api doc/dev/api
git add doc/dev/api
git commit

To update manual:
Edit files in doc/dev/manual

To create a new version:
cp -a doc/dev dev/<version>
Then commit and push on Github
