# This file is generated by dune, edit dune-project instead
opam-version: "2.0"
version: "13.0~dev"
synopsis: "Eliom: backward-compatible module names (Eliom_xxx)"
description:
  "Provides the old Eliom_xxx module names (e.g. Eliom_service, Eliom_content) as aliases to the new Eliom.Xxx modules. Use this package to ease migration of existing code."
maintainer: ["dev@ocsigen.org"]
authors: ["dev@ocsigen.org"]
license: "LGPL-2.1-only WITH OCaml-LGPL-linking-exception"
depends: [
  "dune" {>= "3.19"}
  "eliom" {= version}
  "odoc" {with-doc}
]
build: [
  ["dune" "subst"] {dev}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@runtest" {with-test}
    "@doc" {with-doc}
  ]
]
x-maintenance-intent: ["(latest)"]
