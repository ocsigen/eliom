
# Module `Eliom.Types`

Types shared by client and server.

```ocaml
type sitedata = {
  site_dir : string list;
  site_dir_string : string;
}
```
```ocaml
type server_params
```
```ocaml
val sp : server_params
```