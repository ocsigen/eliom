Kroko

This is alpha-code.
Do not redistribute without permission.

------------------------------------------------------------------
Instructions :

Paquets  nécessaires pour compiler  : 
ocaml
ocamlnet (en fait seulement le module netstring) 
(et lui-même a besoin de pcre ?)

Il faut faire :

  make depend
  make 

pour compiler
puis lancer le serveur :

  cd bin
  ./server 2222

(en remplaçant 2222 par un numéro de port quelconque)

Puis se connecter sur ce port depuis un navigateur :

http://localhost:2222/

Voir le fichier moduleexample/moduleexample.ml pour un tutoriel
qui est pour l'instant chargé par défaut au démarrage du serveur.

Pour l'extension de syntaxe xhtml, voir des exemples dans
xmlp4/exemple/essai.ml

For the while, there is no config file telling which module to load.
To add a module, (for example moduleexample.cmo),
load it from server/krokoserver.ml (end of the file), 
then recompile the server (make server).

To see the list of modules loaded by default, see the file krokoserver.ml


__________________________________________________________________
Krokoutils :

Krokoutils are a set of tools to make creation of pages easier:
 - Cached access to the database without sql (very basic)
 - Persistant data
 - Users and rights management 
 - Common pages components (login box, etc)
 - Save page layout in the database

You don't need Krokoutils to use Kroko. 
Krokoutils is at early stage of development, and could be improved a lot.

To use Krokoutils,
 - install postgresql, create a database named krokobase
   (and give the rights to write in to the user that will launch kroko)
 - install the postgresql and ocamldbi (with postgresql support) libraries 
   for ocaml. (Markus Mottl version of postgresql, not Alain Frish's)
 - compile krokoutils ("make krokoutils")
 - add lines at the end of krokoserver.ml to load krokoutils modules
   (and then "make server")
 - create the tables : psql -f krokoutils/createdb.sql krokobase


------------------------------------------------------------------
Kroko
------------------------------------------------------------------
Written by:

Vincent Balat (project leader, xml parser, kroko.ml, Krokoserver, 
               moduleexample, krokoutils...)
Denis Berthod (http protocol, Krokoserver)
Jérôme Vouillon (lwt library, from unison)
Julien Mineraud (xhtml pretty-printing)

Acknowledgements:

Emmanuel Chailloux
Jean-Vincent Loddo
Jérôme Vouillon
Grégoire Henry
Christophe Fouqueré
Juliusz Chroboczek

and thanks to my (other) students who took part to the very first version:

Yann Binot
Julien Chapuis
Arnaud Delattre
Alexandre Erisay

